<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.min.js" ></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js"></script>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">

</head>
<body>

    <div class="container mt-5 pt-5 justify-content-center align-items-center" style="max-width: 1140px;">

        <div class="row mt-5 pt-5">

            <div class="col col-md-4">
                <div class="card border-2">
                    <img class="card-img-top " src="<%=userInfo.picture%>" alt="Card image cap">
                    <div class="card-body text-center">
                      <h5 class="card-title"><%=userInfo.name%></h5>
                      <a href="/logout" class="btn btn-outline-dark border-1 rounded-0 border-secondary text-center">
                        <i class="bi bi-arrow-down"></i> Sign Out
                    </a>
                    </div>
                </div>
            </div>

            <div class="col col-md-8 bg-light p-3 border rounded-2 border-2">
                <form action="/file/upload" method="POST" enctype="multipart/form-data">
                    <div class="mt-3">
                          <div>
                            <label for="fileUpload" class="form-label">Select an Image to upload your Google Drive</label>
                            <input class="form-control form-control-lg rounded-0" name="fileUpload" id="fileUpload" type="file">
                          </div>
                        <button type="submit" class="btn btn-primary my-3 rounded-0" style="width: 200px;" >
                            <i class="bi bi-image"></i> Upload
                        </button>
                    </div>

                    <% if(typeof status !== 'undefined') { %>
                        <div class="mt-3 alert rounded-0 py-2 text-sm-center alert-success alert-dismissible lead">
                            <%= status %>
                        </div>
                    <% } %>
                </form>
            </div>

            <div class="col col-md-12 mt-3">
                <a href="/image-list" class="btn btn-dark border-2 rounded-0">
                    <i class="bi bi-arrow-90deg-right"></i> Show Upladed Images
                </a>
            </div>

        </div>

    </div>
    
</body>
</html>